{
  "name": "the-clubs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "kill-ports": "node scripts/kill-ports.cjs",
    "db:start": "pnpm --filter @the-clubs/api db:start",
    "db:stop": "pnpm --filter @the-clubs/api db:stop",
    "db:reset": "pnpm --filter @the-clubs/api db:reset",
    "db:migrate": "pnpm --filter @the-clubs/api db:migrate",
    "db:seed": "pnpm --filter @the-clubs/api seed",
    "dev": "pnpm demo:dev",
    "dev:runner": "pnpm kill-ports && concurrently --names api,ck,er,od -c blue,green,magenta,cyan \"cross-env FLOW_COMMANDS=true ALLOWED_ORIGINS=* pnpm --filter @the-clubs/api dev\" \"pnpm --filter customer-kiosk dev\" \"pnpm --filter employee-register dev\" \"pnpm --filter office-dashboard dev\"",
    "dev:fast": "concurrently --names api,ck,er,od -c blue,green,magenta,cyan \"cross-env FLOW_COMMANDS=true ALLOWED_ORIGINS=* pnpm --filter @the-clubs/api dev\" \"pnpm --filter customer-kiosk dev\" \"pnpm --filter employee-register dev\" \"pnpm --filter office-dashboard dev\"",
    "demo:seed": "cross-env DEMO_MODE=true pnpm --filter @the-clubs/api exec tsx src/db/seed-demo/simulator.ts",
    "demo:seed:init": "pnpm --filter @the-clubs/shared build && cross-env DEMO_MODE=true pnpm db:start && cross-env DEMO_MODE=true pnpm --filter @the-clubs/api db:migrate && cross-env DEMO_MODE=true pnpm --filter @the-clubs/api seed && pnpm demo:seed",
    "demo:seed:fresh": "pnpm --filter @the-clubs/shared build && cross-env DEMO_MODE=true pnpm db:reset && cross-env DEMO_MODE=true pnpm --filter @the-clubs/api db:migrate && cross-env DEMO_MODE=true pnpm --filter @the-clubs/api seed && pnpm demo:seed",
    "demo:dev": "pnpm demo:seed:init && cross-env DEMO_MODE=true SKIP_DEMO_SEED=true pnpm dev:runner",
    "demo:dev:fresh": "pnpm demo:seed:fresh && cross-env DEMO_MODE=true SKIP_DEMO_SEED=true pnpm dev:runner",
    "demo:reset": "cross-env DEMO_MODE=true DEMO_RESET_ON_STARTUP=true SEED_ON_STARTUP=false FLOW_COMMANDS=true pnpm dev:runner",
    "build": "pnpm -r run build",
    "test": "pnpm build && pnpm -r run test",
    "lint": "pnpm build && pnpm -r run lint",
    "typecheck": "pnpm build && pnpm -r run typecheck",
    "setup:preline": "bash scripts/setup-preline.sh"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.27.0",
  "devDependencies": {
    "@tailwindcss/postcss": "^4.2.0",
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0",
    "ts-morph": "^27.0.2",
    "turbo": "^2.8.10",
    "typescript": "^5.9.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "bcrypt",
      "esbuild"
    ],
    "overrides": {
      "minimatch": ">=10.2.3",
      "tar": ">=7.5.11",
      "flatted": ">=3.4.2",
      "rollup": ">=4.59.0",
      "@eslint/eslintrc>ajv": "^6.14.0",
      "esbuild": ">=0.25.0",
      "glob": ">=11.1.0"
    }
  }
}